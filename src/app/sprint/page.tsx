import SprintPlanningView from '@/components/SprintPlanningView';

export const metadata = {
  title: 'Sprint Planning | Adsomnia Talk-to-Data Agent',
  description: 'Track sprint progress for the Adsomnia automation agent project',
};

export default function SprintPage() {
  return <SprintPlanningView />;
}

